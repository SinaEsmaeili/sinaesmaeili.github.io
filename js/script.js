const Typed = require('typed.js');

var typed = new Typed('.lt-header', {
    strings: ["Languages and Technologies"],
    typeSpeed: 50,
    backSpeed: 50,
    showCursor: false,
    backDelay: 5000,
    startDelay: 1000,
    loop: true
});